export default interface Product {
    id: number;
    title: string;
    price: number;
    rating: number;
    description: string;
    category: string;
    thumbnail: string;
    images: string[];
}